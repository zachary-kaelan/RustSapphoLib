mod models;
mod comps;
mod consts;

pub use crate::models::BNumber;
pub use crate::models::BnumGroup;


pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
